import 'package:flutter/material.dart';

class LoadingHomeUnitItem extends StatelessWidget {
  const LoadingHomeUnitItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
